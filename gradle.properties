android.useAndroidX=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# Stable resource IDs are not required; non-transitive R classes keep the
# minified build lean.
android.nonTransitiveRClass=true
