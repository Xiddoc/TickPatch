/*
 * Root build script. Plugins are declared here (versions pinned in
 * settings.gradle.kts pluginManagement) and applied in :app.
 */
plugins {
    id("com.android.application") apply false
    id("org.jetbrains.kotlin.android") apply false
}
