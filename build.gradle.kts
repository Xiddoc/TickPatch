/*
 * Root build script. Plugins are declared here (versions in
 * settings.gradle.kts pluginManagement) and applied in the :app module.
 */
plugins {
    id("com.android.application") apply false
    id("org.jetbrains.kotlin.android") apply false
}
